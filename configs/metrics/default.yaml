name: default
quality:
  compute_reference_metrics: true
  compute_no_reference_proxies: true
  compute_asr: false
no_reference:
  device: auto
  nisqa:
    enabled: true
    device: auto
  dnsmos:
    enabled: true
    personalized: false
    device: auto
    num_threads: 1
  squim:
    enabled: true
    device: auto
  plcmos:
    enabled: false
guardrails:
  max_dnsmos_sig_regression: 0.08
  max_quality_regression: 0.08
  max_clipping_pct_increase: 0.001
  max_processing_rtf: 0.85
  max_cleanish_hf_ratio_delta: 0.20
leveling:
  target_window_db: 3.0
samples:
  export_intermediate: false
